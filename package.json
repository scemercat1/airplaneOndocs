{
  "name": "airplaneON-docs",
  "private": true,
  "scripts": {
    "build": "docusaurus build",
    "start": "docusaurus serve --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "@docusaurus/core": "^3.1.0",
    "@docusaurus/preset-classic": "^3.1.0"
  }
}

